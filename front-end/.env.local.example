# Public — sent to the browser. Used by lib/api.ts client-side.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-only — used by lib/session.ts and middleware.ts when calling the API
# from server components / middleware.
API_URL=http://localhost:4000
